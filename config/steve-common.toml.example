[ai]
    # AI provider to use: deepseek, groq, openai, or gemini
    provider = "deepseek"

[deepseek]
    # DeepSeek API key. Do not commit real keys.
    apiKey = ""

    # OpenAI-compatible DeepSeek API base URL
    baseUrl = "https://api.deepseek.com"

    # Common options: deepseek-chat, deepseek-reasoner
    model = "deepseek-chat"

    # Maximum tokens per DeepSeek request
    maxTokens = 4000

    # Temperature (0.0-2.0, lower is more deterministic)
    temperature = 0.7

[openai]
    # Optional fallback/provider-specific API key. Do not commit real keys.
    apiKey = ""

    # Model name for OpenAI-compatible fallback providers
    model = ""

    # Maximum tokens per API request
    maxTokens = 8000

    # Temperature (0.0-2.0, lower is more deterministic)
    temperature = 0.7

[behavior]
    # Ticks between action checks (20 ticks = 1 second)
    actionTickDelay = 20

    # Allow Steves to respond in chat
    enableChatResponses = true

    # Maximum number of Steves that can be active simultaneously
    maxActiveSteves = 10

[minemind]
    # All MineMind features are disabled by default
    autonomousModeDefault = false
    enableChatGuidance = false
    enableCommunityMode = false
    enableLongTermMemory = false

    # 20 ticks = 1 second
    thinkIntervalTicks = 200

    # Maximum task steps a MineMind planner may emit
    maxPlanningSteps = 3

    # Keep false until the safe LLM planner phase is implemented
    useLlmPlanner = false

    # Minimum ticks between MineMind chat replies from the same Steve
    chatResponseCooldownTicks = 100
